import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    titleText: {
        fontSize: 20,
    },
    cardImageContainer: {
        width: 90,
        height: 90,
        borderRadius: 45,
        borderWidth: 3,
        borderColor: 'black',
        overflow: 'hidden',
    },
    image: {
        width: '100%',
        height: '100%',
    }
});